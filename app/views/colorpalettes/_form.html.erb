<%= form_for(@colorpalette) do |f| %>
  <% if @colorpalette.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@colorpalette.errors.count, "error") %> prohibited this colorpalette from being saved:</h2>

      <ul>
      <% @colorpalette.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
